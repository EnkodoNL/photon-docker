# Docker Compose Environment Variables
# Copy this file to .env and modify the values as needed

# Host port to expose the application (maps to container port 2322)
HOST_PORT=2322

# Host directory to mount for data storage (maps to /photon/photon_data in container)
HOST_DATA_PATH=./data